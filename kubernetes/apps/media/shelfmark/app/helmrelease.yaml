---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app shelfmark
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
  driftDetection:
    ignore:
      - paths: [/spec/replicas]
  values:
    controllers:
      *app :
        annotations:
          reloader.stakater.com/auto: 'true'

        initContainers:
          init:
            image: &image
              repository: ghcr.io/calibrain/shelfmark
              tag: v1.2.1@sha256:5e00d47cccaa3b67234855d950d016c50691b78197a68adf15a624f6c08acee2
            command:
              - bash
              - -c
              - 'exec cp -r /usr/local/lib/python3.14/site-packages/seleniumbase/drivers/* /mnt'
            securityContext: &securityContext
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: { drop: ['ALL'] }

        containers:
          app:
            image: *image
            env:
              FLASK_PORT: &port 80
              DEBUG: false
              BOOK_LANGUAGE: en
              CUSTOM_DNS: system

              USING_EXTERNAL_BYPASSER: true
              EXT_BYPASSER_URL: http://flaresolverr.media.svc.cluster.local:8191
              BYPASS_WARMUP_ON_CONNECT: false

              PROWLARR_ENABLED: true
              PROWLARR_URL: http://prowlarr.media.svc.cluster.local
              PROWLARR_TORRENT_CLIENT: qbittorrent
              QBITTORRENT_CATEGORY_AUDIOBOOK: audiobooks
              PROWLARR_USENET_CLIENT: sabnzbd
              SABNZBD_URL: http://sabnzbd.media.svc.cluster.local

              DISABLE_LOCAL_AUTH: true
              OIDC_AUTO_REDIRECT: true

              CALIBRE_WEB_URL: https://kavita.tholinka.dev
              BOOKS_OUTPUT_MODE: folder
              INGEST_DIR: &ingest /ingest
              FILE_ORGANIZATION: organize
              FILE_ORGANIZATION_AUDIOBOOK: organize
              TEMPLATE_ORGANIZE: '{Author}/{Title}/{Author} - {Title} ({Year})'
              TEMPLATE_AUDIOBOOK_ORGANIZE: '{Author}/{Title}/{Author} - {Title} ({Year}) {PartNumber}'
              HARDLINK_TORRENTS: true
              HARDLINK_TORRENTS_AUDIOBOOK: true

              AUTO_OPEN_DOWNLOADS_SIDEBAR: true
            envFrom:
              - secretRef:
                  name: *app
            command: # don't run annoying init script
              - gunicorn
              - --log-level
              - info
              - --access-logfile
              - '-'
              - --error-logfile
              - '-'
              - --worker-class
              - geventwebsocket.gunicorn.workers.GeventWebSocketWorker
              - --workers
              - '1'
              - -t
              - '300'
              - -b
              - 0.0.0.0:80
              - shelfmark.main:app
            probes:
              liveness: &probe
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /request/api/status
                    port: *port
              readiness: *probe
              startup: *probe
            securityContext: *securityContext
            resources:
              requests:
                cpu: 5m
                memory: 128Mi
              limits: # some of the cloudflare attempts can really spike usage
                cpu: 1
                memory: 1Gi
    defaultPodOptions:
      automountServiceAccountToken: false
      enableServiceLinks: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: 'OnRootMismatch'

    service:
      app:
        ports:
          http:
            port: *port

    route:
      app:
        hostnames: ['{{ .Release.Name }}.tholinka.dev']
        parentRefs:
          - name: envoy-internal
            namespace: network

    persistence:
      config:
        existingClaim: *app
        advancedMounts:
          *app :
            app:
              - path: /config

      downloads:
        forceRename: '{{ .Release.Name }}-downloads'
        storageClass: ceph-block
        accessMode: ReadWriteOnce
        size: 2Gi
        advancedMounts:
          *app :
            app:
              - path: /app/downloaded_files

      tmpfs:
        type: emptyDir
        advancedMounts:
          *app :
            app:
              # chromium needs the XDG_CONFIG_HOME created
              # because user 1000 doesn't exist, ~ = /
              - path: /.config
                subPath: config
              - path: /tmp
                subPath: tmp
              - path: /var/log
                subPath: log
              - path: /usr/local/lib/python3.10/site-packages/seleniumbase/drivers
                subPath: drivers
            init:
              - path: /mnt
                subPath: drivers
      media:
        type: nfs
        server: nas.servers.internal
        path: /media
        advancedMounts:
          *app :
            app:
              - path: *ingest
                subPath: book
